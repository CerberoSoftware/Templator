
.DS_Store
*.log
node_modules/

# Vite build output, generated by `npm run build`. index.php reads
# public/static/.vite/manifest.json at runtime, so this is built on deploy
# rather than committed.
public/static/
